<rules>
<rule>
condition	= var(value) > 0.99 
fill_color	= vvdred
</rule>
<rule>
condition	= var(value) > 0.9
fill_color	= orange
</rule>
<rule>
condition	= var(value) > 0.8
fill_color	= yellow
</rule>
<rule>
condition	= var(value) > 0.7
fill_color	= vlyellow
</rule>
<rule>
condition	= var(value) > 0.6
fill_color	= lgreen
</rule>
<rule>
condition	= var(value) > 0.31
fill_color	= green
</rule>
<rule>
condition	= var(value) > 0.2
fill_color	= vdgreen
</rule>
#<rule>
#condition	= var(value) < 0.1
#value		= 0.2
#color		= dred
#</rule>
</rules>